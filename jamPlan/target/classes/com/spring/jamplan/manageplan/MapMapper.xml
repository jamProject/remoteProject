<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
CREATE TABLE PLANMAP(
PLANNO NUMBER NOT NULL,
ID VARCHAR2(50),
USERCOLOR VARCHAR2(50),
MAPCOUNT NUMBER DEFAULT 0,
LOCATION VARCHAR2(100)
);
 -->
<mapper namespace="com.spring.jamplan.manageplan.MapMapper">
	
	<resultMap type="com.spring.jamplan.model.MapVO" id="mapMemberResultMap">
		<result property="planNo" column="PLANNO" />
		<result property="id" column="ID" />
		<result property="pickCount" column="PICKCOUNT" />
		<result property="userColor" column="USERCOLOR" />
		<result property="location" column="LOCATION" />
	</resultMap>
	
	<!-- controller에서 list의 length는 그장소에 대한count -->
	<select id="getPickList" parameterType="String" resultMap="mapMemberResultMap">
		select * from planMap where location=#{location, javaType=String}  
	</select>
	
	<select id="checkPick" parameterType="com.spring.jamplan.model.MapVO" resultType="int">
		select count(*) from planMap where planNo=#{planNo, javaType=int} and 
			id=#{id, javaType=String} and location=#{location, javaType=String}
	</select>
	
	<insert id="insertMember" parameterType="com.spring.jamplan.model.MapVO" >
		insert into planMap values
			(#{planNo, javaType=int}, #{id, javaType=String}, #{userColor, javaType=String},
			    #{pickCount, javaType=int}, #{location, javaType=String})
	</insert>
	
	<delete id="deleteMember" parameterType="com.spring.jamplan.model.MapVO">
		delete from planMap where planNo=#{planNo, javaType=int} and id=#{id, javaType=String} 
			and location=#{location, javaType=String}
	</delete>
	
	<select id="pickCount" parameterType="com.spring.jamplan.model.MapVO" resultType="int">
		select count(*) from planMap where planNo=#{planNo, javaType=int} 
			and	location=#{location, javaType=String}
	</select>
	
	<update id="updatePickCount" parameterType="com.spring.jamplan.model.MapVO" >
		update planMap set pickCount=#{pickCount, javaType=int} 
		where planNo=#{planNo, javaType=int} 
		and	location=#{location, javaType=String}
	</update>
	<!-- 지도오른쪽리스트 : 
	<select>
		select location from planMap group by location order by count(location) desc;
	</select>
	 -->
</mapper> 
 